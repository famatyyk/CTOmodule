# MVP v0.2 — CTOmodule

Data: 26 stycznia 2026

## Cele
1) Bezpieczny dev reload bez restartu klienta  
2) Log w UI (nie tylko printy)  
3) Konfiguracja z `config/default.lua` + opcjonalne override w `config/user.lua`

## Co doszlo
### 1) `CTOmodule.reload()`
- Przycisk **Reload** w UI
- W konsoli: `CTOmodule.reload()`

### 2) Log panel w oknie
- `CTOmodule.log("...")` dopisuje linie do bufora i do UI
- Przycisk **Clear log**

### 3) Config
- `config/default.lua` jest ladowany zawsze
- `config/user.lua` (opcjonalny) nadpisuje defaults

## Test (OTClient)
```lua
dofile('modules/CTOmodule/init.lua')
init()

CTOmodule.log('hello')
CTOmodule.toggle()
CTOmodule.reload()
```

Hotkey: `Ctrl+Shift+C`
